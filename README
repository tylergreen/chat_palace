Chat Palace is a webapp designed to show off the power of Meteor.js and friends.

* Requirements:
First install meteor (see meteor.com) and meteorite (npm install -g meteorite)

how to run:
$ mrt run

how to test:
$ mocha --compilers coffee:coffee-script tests

how to deploy

$ mrt deploy chat_palace.meteor.com
